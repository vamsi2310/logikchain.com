design.md or plan.md: Breaks down the technical requirements. It specifies how the architecture should look, database structure, and integration constraints.


database is firebase. 

use firebase authentication

create users table entry for each new signup. 

when a user is logged in, based on the role in the users table, the user is redirected to corresponding user specific homepage.

a user with one type of role can not access any other role pages. 