# Copy to .env.dev / .env.test / .env.prod (not committed).
# One alias per file. Secrets go in Secret Manager, not here.
# FIREBASE_PROJECT_ALIAS=dev
FUNCTIONS_REGION=asia-south1
AUTH_ID_TOKEN_TTL_SECONDS=3600
PENDING_REPAYMENT_RELIEF_CEILING_HOURS=48
BENEFICIARY_COOLING_HOURS=24
